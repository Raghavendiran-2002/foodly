class UIDTracker {
  //uid also available via auth.instance... but this is need to enable debug via emulator (no otp auth support)
  //done by giving hardcoded uid to emulator and using it everywhere
  static String? userUID;
  static String? vendorID;
  static String? userDocID;
}
